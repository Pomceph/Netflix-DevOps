apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm-netflix-devops.fullname" . }}
  labels:
    {{- include "helm-netflix-devops.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helm-netflix-devops.selectorLabels" . | nindent 4 }}
